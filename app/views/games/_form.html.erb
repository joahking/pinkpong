<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_left_id %>*<br />
    <%= f.select :player_left_id, @users.collect { |u| [u.email, u.id] } %>
  </div>
  <div class="field">
    <%= f.label :player_right_id %>*<br />
    <%= f.select :player_right_id, @users.collect { |u| [u.email, u.id] } %>
  </div>
  <div class="field">
    <%= f.label :winner_id %>*<br />
    <%= f.select :winner_id, @users.collect { |u| [u.email, u.id] } %>
  </div>
  <div class="field">
    <%= f.label :score %><br />
    <%= f.text_field :score %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
